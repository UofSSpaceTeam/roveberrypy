# lists of the keys of messages each thread wants to receive from network

cameraList = (["vidsource", "takePicture", "cameraMovement"])

telemetryList = ([])

driveList = (["motorSpeeds"])

armList = (["armAbsolute", "armDirect"])

antennaList = (["cameraMovement"])

experimentList = (["drillspd", "elevspd", "drill", "elev", "laser"])

